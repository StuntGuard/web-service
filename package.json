{
  "name": "web-service",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "generate": "npx prisma generate",
    "deploy": "npx prisma migrate deploy",
    "dev": "npm run generate && nodemon --watch \"src/**\" --ext \"js,json\" --exec \"node src/servers/app.js\"",
    "build": "npm run generate",
    "start": "npm run build && node build/servers/app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "nodemon": "^3.1.0",
    "prisma": "^5.14.0",
    "validator": "^13.12.0"
  }
}
